import React from 'react'
import "./investor.css"

const Investor = ({name,stocks,id, key, stockInfo}) => {
  return (
    <section>
        <h1>{name}</h1>
        <div>
            
        </div>
    </section>
  )
}

export default Investor